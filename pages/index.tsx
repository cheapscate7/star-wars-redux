import { NextPage } from 'next';
import Head from "next/head";

type HomeProps = {
};

const Home: NextPage<HomeProps> = () => {
    return (
        <>
            <Head>
                <meta
                    name="Description"
                    content=""
                />
            </Head>
        </>
    );
};

export default Home;